---
##############################
# Setup K8s components
##############################
- hosts: localhost
  connection: local
  roles:
    - ansible-config
    - cert

- hosts: test-machine
  gather_facts: false
  become: yes
  roles:
    - common
    - master

# - hosts: k8s-workers
#   become: yes
#   roles:
#     - common
#     - worker
#
# - hosts: k8s-edges
#   become: yes
#   roles:
#     - common
#     - worker
#     - edge

##############################
# Setup kubectl
##############################
# - hosts: localhost
#   tasks:
#     - name: test
#       raw: "echo a"
#
# - hosts: localhost
#   roles:
#     - kubecontrol
